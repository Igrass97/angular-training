import { AnchorPointDirective } from './anchor-point.directive';

describe('AnchorPointDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchorPointDirective();
    expect(directive).toBeTruthy();
  });
});
